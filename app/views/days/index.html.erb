<h2>予約可能人数</h2>
<%= render @days %>
<%= month_calendar events: @days do |date, days| %>
  <%= date.day %>
  <% days.each do |day| %>
    <div>
      <%= day.capacity %>
    </div>
  <% end %>
<% end %>
